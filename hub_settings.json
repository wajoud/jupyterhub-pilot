{
    "_comment_network": "Replace [HUB_IP] with the private IP of your JupyterHub server.",
    "hub_ip": "[HUB_IP]",
    "proxy_api_url": "http://[HUB_IP]:5432",
    "hub_api_url": "http://[HUB_IP]:8081/hub/api",
    "hub_bind_url": "http://[HUB_IP]:8081",
    "hub_port": 8000,

    "_comment_auth": "Set your team domain and admin usernames here. Admins get full cross-user session access.",
    "hosted_domain": "your-team-domain.com",
    "admin_users": ["admin_username"],

    "_comment_mapping": "Path to the JSON fallback mapping file (used if SQLite DB is unavailable).",
    "mapping_file": "user_mapping.json",

    "_comment_db": "Path to the SQLite state DB. Stores team→VM mappings and live per-user session state.",
    "db_path": "/var/lib/jupyterhub/jupyterhub_pilot_state.db",

    "_comment_resources": "cgroups v2 hard limits enforced via systemd-run on the Worker VM. Set to null to disable.",
    "resource_limits": {
        "memory_max": "1G",
        "cpu_quota": "50%"
    },

    "_comment_vault": "HashiCorp Vault integration. Secrets are read from secret/jupyterhub-pilot/<username>. Set VAULT_ADDR and VAULT_TOKEN env vars on the Hub VM. Disable by omitting or setting vault_enabled to false.",
    "vault_enabled": false,
    "vault_secret_path": "secret/jupyterhub-pilot"
}